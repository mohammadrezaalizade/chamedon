import React from "react";
interface Props {
  children: JSX.Element;
}
const SectionLayout: React.FC<Props> = ({ children }): JSX.Element => {
  return <div>SectionLayout</div>;
};

export default SectionLayout;
